/*
    Operadores de comparação
    > maior que
    >= maior ou igual
    < menor que
    <= menos ou igual
    == igualdade (valor) *** usar menos ele (devido a correção de tipos)
    === igualdade estrita (valor e tipo)
    != diferente (valor) *** não usar
    !== diferente estrito (valor e tipo)

*/
let comp = 15> 5; //a variavel 'com' recebe um valor booleano
console.log(comp);

let num1 = 10;
let num2 = '10';
let res = num1 === num2; //compara valor e tipo
